import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'お客様登録 | 学生服',
}

export default function CrmRegisterLayout({ children }: { children: React.ReactNode }) {
  return children
}
