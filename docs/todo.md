# 今後のToDoリスト

> ここには「大幅な変更を加える前の確認事項」を記録します。

---

## 現在の確認待ち事項（人間に確認が必要）

現時点: なし

---

## 次サイクル（P1）の実装予定

> 実装前に todo.md に記録してから作業を開始します。

### F-04: 採寸会チェックリスト
- 影響ファイル: `app/[storeId]/admin/` 内に新規ページ追加
- 変更規模: 中（新規ページ＋APIルート）
- 確認事項: reservations テーブルへの `purpose` フィールド活用方法

### F-05: 在庫引当・発注数量自動集計
- 影響ファイル: `app/[storeId]/admin/purchase/page.tsx`, 新規APIルート
- 変更規模: 大（既存の purchase ページの大幅改修）
- ⚠️ **確認が必要**: uniform_orders の集計ロジックをどこに置くか（API vs クライアント側）

### F-06: 保護者向けマイページ
- 影響ファイル: `app/[storeId]/home/page.tsx` または新規 `app/[storeId]/mypage/page.tsx`
- 変更規模: 中（新規ページ＋既存CRM APIの活用）
- 確認事項: LIFF認証で line_user_id から顧客レコードを引く既存の仕組みとの整合性

---

## 将来の大規模変更（要相談）

- 在庫管理モジュール（新テーブル設計が必要）
- 発注書のEDI連携（外部API接続）
- 多店舗在庫共有（RLS変更が必要）
