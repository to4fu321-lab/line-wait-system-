-- ============================================================
-- 順番待ちQR POP設定列 (2026-07-10)
--
-- 背景:
--   友だち登録POP(stores.pop_settings)と同じ仕組みで、
--   「未登録でも読み取り→即・順番待ち」に誘導する専用POPを
--   別設定として保存できるようにする。チラシ・店頭・Googleマップ・
--   HP掲載など、画面を直接見せられない媒体向けの出力専用画面用。
--
-- 冪等: 再実行しても安全。
-- ============================================================

ALTER TABLE public.stores
  ADD COLUMN IF NOT EXISTS queue_pop_settings jsonb;

NOTIFY pgrst, 'reload schema';
